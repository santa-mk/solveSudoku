<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="white_grid">
        <item name="android:background">@color/white</item>
        <item name="android:width">@dimen/grid_size</item>
        <item name="android:height">@dimen/grid_size</item>
    </style>

    <style name="gray_grid" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:background">@color/gray</item>
        <item name="android:width">@dimen/grid_size</item>
        <item name="android:height">@dimen/grid_size</item>
    </style>
</resources>
